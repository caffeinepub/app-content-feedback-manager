{
  "frontend/src/App.tsx": "Main app shell with deep-space background, floating geometric shape decorations, redesigned sticky header (logo with glow, countdown timer card, music toggle), four-tab navigation (User View, Upload, Live Checker, Admin), and footer with caffeine.ai attribution.",
  "frontend/src/views/admin/AdminView.tsx": "Admin panel wrapper that gates content behind AdminUnlock, then renders sub-tabs (Comments, AI Templates, Images, Chat, Live List, Settings). Updated to use default imports for AdminAITemplates, AdminLiveList, and AdminSettings to match their export style.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; updated `useGenerateBulkComments` to invalidate `listMetrics` and `availableCount` queries on success, and added a new `useGenerateSingleComment` mutation that calls `generateBulkComments` with count=1 and also invalidates those queries.",
  "frontend/src/hooks/useCommentGenerator.ts": "Utility hook providing generate (single comment) and generateBulk (multiple comments with optional symbol append) functions that randomly sample from a CommentList's templates and append the list suffix.",
  "frontend/src/views/UserView.tsx": "Single Comment Generator view that now uses `useGenerateSingleComment` to atomically consume one template from the shared pool, with optimistic local available count updates immediately after generation, out-of-comments badge when pool is empty, and device lock stored in localStorage.",
  "frontend/src/views/admin/AdminSettings.tsx": "Admin settings panel with an Access Key Management section (view/toggle masked key, set custom key, regenerate random key) above the existing Background Music upload/toggle controls.",
  "frontend/src/views/admin/AdminAITemplates.tsx": "Admin AI template generator with topic, tone, count controls plus a new Append Symbol input that appends any entered symbol to the end of every generated template, with save-to-list functionality.",
  "frontend/src/views/admin/AdminLiveList.tsx": "Admin live list manager with apps/events table (inline rename, delete confirmation), Add Usernames section (textarea + bulk .txt/.csv upload), and Username Checker with multi-select checkboxes and Found-in / Not-found-in results panels — all using correctly named hook imports.",
  "frontend/src/index.css": "Global styles with OKLCH deep-space color system, floating geometric shape animations, `.space-card` (dark semi-transparent card), `.gradient-heading` (neon blue/teal gradient text), `.gradient-button` (teal-to-blue gradient), and `.floating-shapes` / `.app-bg-gradient` background utilities.",
  "frontend/src/views/LiveChecker.tsx": "Redesigned Live Checker tab with multi-username textarea input, Select All/specific app dropdown, per-username results showing count + found/not-found lists, and a public \"Live List for All Users\" collapsible section below.",
  "frontend/src/components/UploadComment.tsx": "New Upload Comment component with a textarea and Upload Comment button that submits to the backend via addChatMessage, with success/error feedback.",
  "frontend/src/components/BulkCommentGenerator.tsx": "Bulk Comment Generator component updated to fetch real available count from backend via `useAvailableCount`, show immediate optimistic count updates after generation, display clamping toast when fewer comments are generated than requested, disable quantity options exceeding available count, and show out-of-comments state when pool is empty.",
  "frontend/src/views/UploadView.tsx": "Restructured Upload tab with four sections in order: Upload Comment, Bulk Comment Generator (access-key-locked), Bulk Comments file upload (renamed from Bulk Uploading Template), and Upload Rating Image — all using space-card styling.",
  "frontend/src/hooks/useDeviceId.ts": "New custom hook that reads or generates a persistent `deviceId` from localStorage using `crypto.randomUUID()`, ensuring the same device ID is reused across sessions.",
  "frontend/src/components/MetricsDonutChart.tsx": "Pure SVG/CSS donut chart component using `stroke-dasharray`/`stroke-dashoffset` to visualize used vs available template proportions; accepts `usedTemplates`, `totalTemplates`, `size`, and `strokeWidth` props with no external chart dependencies.",
  "frontend/src/views/admin/AdminComments.tsx": "Admin comments panel extended with Reset Pool and Reset Pool + Clear Claims buttons per list in the Usage Metrics section, each triggering an AlertDialog confirmation before executing; on confirm, invalidates metrics and available count queries and shows a success toast."
}