{
  "frontend/src/App.tsx": "Main app shell with tabbed UI (Comment, Live, Upload, Admin), a midnight countdown timer displayed at the top center as a pill banner, a light/dark mode toggle button (sun/moon icon) in the header, and background music toggle — uses next-themes `useTheme` for theme switching.",
  "frontend/src/views/admin/AdminView.tsx": "Admin panel wrapper with unlock screen, tabbed navigation (Comments, Live List, Pricing, Earnings, Withdrawals, AI Templates, Images, Chat, Settings), and lock/unlock functionality. Uses named imports for AdminChat and AdminImages to match their export style.",
  "frontend/src/hooks/useQueries.ts": "Comprehensive React Query hooks for all backend operations — all mutation hooks are now fully wired to the actual backend actor methods (createCommentList, deleteCommentList, renameCommentList, addTemplatesToCommentList, lockCommentList/unlockCommentList, createAppEvent, deleteAppEvent, addUsernamesToAppEvent, importLiveLists, addChatMessage, uploadImage, setAccessKey, regenerateAccessKey, setBgMusicEnabled, setMusicUrl, addPriceEntry/editPriceEntry/deletePriceEntry/bulkUploadPrices, setInventoryCount, claimComment, submitWithdrawalRequest, etc.); no more stub errors.",
  "frontend/src/hooks/useCommentGenerator.ts": "Utility hook providing generate (single comment) and generateBulk (multiple comments with optional symbol append) functions that randomly sample from a CommentList's templates and append the list suffix.",
  "frontend/src/views/UserView.tsx": "Single Comment Generator view with device-based claim tracking via localStorage; fixes the `claimComment.mutateAsync` call to pass the required `{ listId, username }` object instead of a bare string, matching the updated hook signature.",
  "frontend/src/views/admin/AdminSettings.tsx": "Admin settings panel with Access Key Management (view/toggle masked key, set custom key, regenerate random key) and Background Music upload/toggle; uses only standard mutation hooks from useQueries.ts with no authenticated actor construction.",
  "frontend/src/views/admin/AdminAITemplates.tsx": "Admin AI template generator with topic, tone, count controls plus a new Append Symbol input that appends any entered symbol to the end of every generated template, with save-to-list functionality.",
  "frontend/src/views/admin/AdminLiveList.tsx": "Admin live list manager with Create App/Event List form, Auto Import & Save using parseLiveListReport, and bulk username upload; the import result handler no longer accesses properties on the mutation return value (which is `never` since the stub always throws), instead showing a generic success message on the happy path and catching errors in the catch block.",
  "frontend/src/index.css": "Global styles with OKLCH color system supporting both dark (default) and light modes via `.dark`/`.light` class selectors; includes countdown banner CSS variables, glassmorphism cards, gradient text/buttons, floating shapes, and all utility animations — all adapted for both themes.",
  "frontend/src/views/LiveChecker.tsx": "Redesigned Live Checker tab with a new pill-style multi-app username checker at the top (single username input, Select All/Clear controls, pill checkboxes per app, teal Check Username button, per-app Found/Not Found results with position and price, earnings summary section), followed by the existing bulk multi-username checker and the Live List for All Users collapsible section.",
  "frontend/src/components/UploadComment.tsx": "New Upload Comment component with a textarea and Upload Comment button that submits to the backend via addChatMessage, with success/error feedback.",
  "frontend/src/components/BulkCommentGenerator.tsx": "Bulk Comment Generator component that uses the local `useCommentGenerator` hook for client-side generation, with access-key validation, quantity selector capped to available templates, and copy-all output — no longer calls the unimplemented backend `generateBulkComments`.",
  "frontend/src/views/UploadView.tsx": "Restructured Upload tab with four sections in order: Upload Comment, Bulk Comment Generator (access-key-locked), Bulk Comments file upload (renamed from Bulk Uploading Template), and Upload Rating Image — all using space-card styling.",
  "frontend/src/hooks/useDeviceId.ts": "New custom hook that reads or generates a persistent `deviceId` from localStorage using `crypto.randomUUID()`, ensuring the same device ID is reused across sessions.",
  "frontend/src/components/MetricsDonutChart.tsx": "Pure SVG/CSS donut chart component using `stroke-dasharray`/`stroke-dashoffset` to visualize used vs available template proportions; accepts `usedTemplates`, `totalTemplates`, `size`, and `strokeWidth` props with no external chart dependencies.",
  "frontend/src/views/admin/AdminComments.tsx": "Admin comments panel with Create Comment List form, full CRUD for comment lists, expandable template management, and Usage Metrics with donut charts; uses only standard mutation hooks from useQueries.ts with no authenticated actor construction.",
  "frontend/src/hooks/useClaimedComments.ts": "New custom hook that manages per-device, per-list claimed comments using localStorage, with keys formatted as `claimed_comment_{deviceId}_{listId}`. Exposes `getClaimedComment`, `storeClaimedComment`, and `hasClaimedComment` functions using the device ID from `useDeviceId`.",
  "frontend/src/utils/liveListParser.ts": "New utility module that exports `parseLiveListReport(text)` — parses \"REVIEWS WORLD Reports\" formatted text to extract app/event names (wrapped in asterisks), grouped usernames (with numbering stripped), and optional date metadata, returning a structured `ParseResult` with success flag, entries array, and error message.",
  "frontend/src/views/admin/AdminPricing.tsx": "Admin view for managing the App Price List with add/edit form, price entries table, and bulk upload textarea; uses only standard mutation hooks from useQueries.ts with no authenticated actor construction.",
  "frontend/src/views/admin/AdminEarnings.tsx": "Admin earnings view showing auto-calculated summary cards (apps, entries, total earnings), per-app breakdown table with totals row, and all app events reference; uses only standard query hooks from useQueries.ts with no authenticated actor construction.",
  "frontend/src/hooks/useAdminAuth.ts": "Admin auth hook that persists admin state in localStorage (keys: adminCode, isAdmin), restores state on mount via useEffect, and exposes unlock/lockAdmin functions — no page reload or URL injection, purely state-based.",
  "frontend/src/components/AdminUnlock.tsx": "Admin unlock screen with password input that auto-skips if localStorage already has valid adminCode/isAdmin, calls useAdminAuth's unlock function, and invokes onUnlocked callback on success.",
  "frontend/src/main.tsx": "Application entry point that wraps the app with QueryClientProvider and ThemeProvider (next-themes, defaultTheme dark) to enable light/dark mode switching."
}